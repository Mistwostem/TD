- lecteur fichier
	* Detecteur du nombre de lignes
	* Gérer les espaces, point, ect
	* Parser les mots
	
- indexeur (MAP avec clé = mot et nombre = nb occurrences)
	* Ordonner les mots alphabétiquement
	* Compte les occurrences des mots
	* Prendre en compte les mot répétés sur la même ligne

- Lecteur / Parseur -> indexeur


** FONCTIONNALITES **

ExtracteurMots pour le lecteur.
	-> classe motSuivant() retrourne les mot et sa ligne
		-> retourne le numéro de la colonne ou le mot commence 
		-> exemple : matin 3 10
		-> Renvoyer le mot en majuscule meme si on le souhaite pas dans l'indexeur
	
			-> Méthode infoMot() 
				-> renvoi le mot (string)
				-> le numéro de ligne (int)
				-> le numéro de colonnes (int)
				
Index relie des mots avec des lignes. 
	-> ajout(String mot, int n°ligne)
	-> supprimer(mot)
	-> supprimer(mot, nbLigne) [surchargée]
	-> rechercher(mot) => retourne les lignes ou apparaissent ce mot
	-> rechercher(mot, ligne) => retourne un booléen
	-> nbMots() 
	-> vider()
	-> estVide()
	-> tousLesMots() => retourne les mots

IndexImp + ExtracteurImp.